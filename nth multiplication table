//print nth multiplication table.Read n value from the user
#include<stdio.h>
int main()
{
	int i, number;
	printf("Enter a number:");
	scanf("%d", &number);
	for(i=1; i<=20; i++)
	{
		printf("%d * %d = %d\n", number, i, (number*i));
	}
}
